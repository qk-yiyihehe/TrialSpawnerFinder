pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'TrialSpawnerFinder'

include 'finder-core'
include 'geode-finder'
